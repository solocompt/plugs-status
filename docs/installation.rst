============
Installation
============

At the command line::

    $ easy_install plugs-status

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-status
    $ pip install plugs-status
